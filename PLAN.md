# 装箱单小程序 - 分步计划

> 默认入口：从「已有填写好的 Excel 表」开始，解析后展示为淘宝式商品卡片。下单仍线下用 Excel，不在小程序内下单。

---

## 一、整体流程回顾

1. **中间商** 给商店空白报价单  
2. **商店** 填写报价单后交给中间商 → 装箱单  
3. **中间商** 把装箱单 Excel 上传到小程序  
4. **小程序** 把 Excel 转成「类似淘宝」的展示页  
5. **功能**：展示、加购、看图与详情、购物车导出 Excel、按时间/类别排序  
6. **不下单**：客户仍拿 Excel 线下下单  

---

## 二、Step by Step 执行计划（按顺序做）

| 步骤 | 内容 | 说明 |
|------|------|------|
| **Step 1** | 定义 Excel 表头与 JSON 结构 | 根据你提供的装箱单表头，写出 `excelSchema.json`（列名、字段 key、类型、是否必填），以及单行商品对应的 JSON 结构。 |
| **Step 2** | 实现 Excel 解析器 | 在 `miniprogram/utils/excelParser.js` 中：读取 Excel（从 data 行开始，跳过第 1–2 行说明），按 schema 映射到商品对象数组；处理图片列（若为内嵌图片则记路径或占位，若为 URL 则保留）。 |
| **Step 3** | 默认从「已解析数据」开始 | 不做上传，先支持：从本地/全局已解析好的商品列表渲染。即默认流程 = 「已有填写好的 Excel 表」→ 解析得到 JSON → 直接进入淘宝式列表页。上传 Excel 的入口可后续再加。 |
| **Step 4** | 商品列表页（淘宝式卡片） | 新建页面：商品列表，用 `excelSchema` 解析后的数据渲染卡片（图、中文名、单价、规格、颜色等），支持点击进详情。 |
| **Step 5** | 商品详情页 | 展示单品的图片、名称、规格、颜色、材质、单价、箱数、总数量、金额、证书、条形码、箱规等；有「加入购物车」按钮。 |
| **Step 6** | 购物车 | 购物车页：列表展示已加购商品、可改数量、删除；底部有「导出 Excel」按钮。 |
| **Step 7** | 购物车导出 Excel | 按装箱单表头格式，把购物车内容导出为 Excel 文件（用 xlsx 写表头 + 数据行），供客户线下下单使用。 |
| **Step 8** | 排序与筛选 | 列表/购物车支持：按时间顺序、按类别（如按店铺 shopNo、按货号、按中文名分类）排序；筛选如「近一周的新品」（若有上传时间或批次字段则按之筛选，否则按解析批次/导入时间）。 |
| **Step 9** | 上传 Excel 入口（可选） | 中间商上传装箱单 Excel：选择文件 → 调用解析器 → 得到商品列表 → 写入全局/本地 → 跳转商品列表页。可与 Step 3 的默认流程合并为「有数据就展示，无数据可先上传」。 |

---

## 三、当前约定

- **表头**：以你提供的 Excel 表头为准（第 1–2 行保留不解析，数据从第 3 或第 4 行表头下一行开始）。  
- **默认流程**：从「已有填写好的 Excel 表」开始 = 从「解析后的 JSON 商品列表」直接展示淘宝式卡片；上传 Excel 可作为后续步骤接入。  
- **Excel 的 JSON 与解析**：见 `miniprogram/utils/excelSchema.json` 与 `miniprogram/utils/excelParser.js`。  

---

## 四、xlsx 依赖（Step 7 / Step 9）

小程序端**不能直接使用 npm 的完整 xlsx 包**，需使用浏览器版精简包：

1. 从 SheetJS 官方 dist 目录获取 **xlsx.mini.min.js**（或从 [SheetJS CDN](https://cdn.sheetjs.com/) 等渠道下载浏览器用精简版）。
2. 将 **xlsx.mini.min.js** 放到 **miniprogram/utils/** 目录下。
3. 上传页与购物车导出会通过 `require('../../utils/xlsx.mini.min.js')` 使用；若未放置该文件，会提示「请将 xlsx.mini.min.js 放入 utils 目录」。

---

你确认要执行时，我从 **Step 1（schema + 单行 JSON 结构）** 和 **Step 2（解析器）**、**Step 3（默认从解析数据展示）** 开始做；然后再做 Step 4 淘宝式列表页。
